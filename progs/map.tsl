
//Maps involution f to all elements of List
involution map List
    if List = 'nil
    then skip
    else NonEmpty ^= 'true
         (Head.Tail) <- List
    fi NonEmpty
    if NonEmpty
    then skip
    else (Head.Tail) <- (involute f Head. involute map Tail)
    fi NonEmpty
involution f X
    X <- X